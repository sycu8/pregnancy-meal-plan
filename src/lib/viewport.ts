import type { Viewport } from "next";

/** Explicit mobile viewport for both EN and VI root layouts. */
export const siteViewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  themeColor: "#fffaf5"
};
